{
  "name": "encryptor-web-app",
  "version": "1.0.0",
  "description": "The server of the app. The main directory of the app.",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon app.js",
    "install-client": "npm install --prefix encryptor",
    "build-client": "npm run build  --prefix encryptor",
    "client": "npm start --prefix encryptor",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm install --prefix encryptor && npm run build --prefix encryptor"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/VictorMegir/Encryptor-web-app.git"
  },
  "author": "Victor Megir",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/VictorMegir/Encryptor-web-app/issues"
  },
  "homepage": "https://github.com/VictorMegir/Encryptor-web-app#readme",
  "dependencies": {
    "express": "^4.17.1",
    "nodemon": "^2.0.7",
    "serve-favicon": "^2.5.0"
  }
}
